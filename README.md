## 🚀 Instalación y Ejecución
Este proyecto fue migrado a **Yarn**. Asegúrate de tenerlo instalado antes de continuar.

### 1. Clonar el proyecto
git clone <url-de-tu-repositorio>
cd <nombre-de-tu-carpeta>

### 2. Instalar dependencias
yarn install

### 3. Iniciar el servidor de desarrollo
yarn start

¿Por qué es importante cambiarlo?
Consistencia: Si alguien usa npm install en un proyecto que tiene un archivo yarn.lock, podría generar conflictos de versiones entre librerías.
Velocidad: Yarn lee el archivo yarn.lock para instalar todo mucho más rápido de lo que lo haría NPM desde cero.
Profesionalismo: Indica que el proyecto tiene un flujo de trabajo definido.
